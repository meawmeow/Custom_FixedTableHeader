<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="th.co.businessplus.kotlinui1">


    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".custom_table.LibQTableJavaActivity"></activity>
        <activity android:name=".custom_table.LibQTableFixSizeJavaActivity" />
        <activity android:name=".pagination_data.PaginationQActivity" />
        <activity android:name=".pagination_data.PaginationActivity" />
        <activity android:name=".custom_table.LibQTableActivity" />
        <activity android:name=".custom_table_report.TableReportActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>